import React from "react";

const Header = () => {
  return (
    <div className="mt-5 mb-3">
      <h2 className="text-white text-center">Paste a link to shorten it</h2>
    </div>
  );
};

export default Header;
